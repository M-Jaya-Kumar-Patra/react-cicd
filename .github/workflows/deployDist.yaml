name: Deploy Dist
on: [push, workflow_dispatch]
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Manual Clone
              run: |
                echo "Cloning repo..."
                git clone https://github.com/${{ github.repository }}.git